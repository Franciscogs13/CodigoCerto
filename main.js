var typed = new Typed("#text", {
  strings:["Desenvolvedor Front-End"],
  typeSpeed:100,
  backSpeed:100,
  backDelay:1000,
  loop:true
})

document.querySelector(anchor.getAttribute('href').slice(1)).scrollIntoView({
  behavior: 'mooth'
});